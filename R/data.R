#' cmr_dat
#'
#' @format A data frame with five variables: \code{id}, \code{loc},
#'   \code{x}, \code{y} and \code{date}. \code{cmr_dat} is an example of how dataframes should be created to feed into CMRdat.
"cmr_dat"

#' cmr_dat2
#'
#' @format A data frame with six variables: \code{id}, \code{loc},
#'   \code{x}, \code{y}, \code{date} and \code{layer}. \code{cmr_dat2} is an example of how dataframes should be created to feed into CMRdat when there are multiple layered networks (i.e. a separate network for males and females).
"cmr_dat2"
